Physical DTA21277 video follow-up: selection presentation must explicitly preserve the cabinet topology 1|3 / 2|4 regardless of inherited CSS/order, and the `starting` transaction scene is visually under-designed (large empty canvas with only spinner/text). Both fixes are presentation-only. No slot number sent to backend, pricing, payment, rental-session, ejection or hardware behavior may change.