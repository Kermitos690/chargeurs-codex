# P0 physical proof governance

The `kiosk-p0-physical-proof.css` file is intentionally tiny and temporary. It exists because physical DTA21277 evidence exposed a higher-specificity legacy Home footer rule that could not be safely removed during the active field loop without rebuilding the larger reference stylesheet. Once the physical Home is validated, this rule must be folded into the owning Home/core stylesheet and the temporary file deleted.
