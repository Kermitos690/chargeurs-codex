# Production remaining gates

Production is blocked until staging CORS/function/frontend deployment, DTA QR
display, hosted Stripe Test Checkout, signed webhook replay, expiry,
cancellation, refund, controlled physical release/return, email delivery,
Stripe branding/legal data, APK signing/update compatibility and a secret/RLS
review are evidenced. Stripe Live stays disabled until sign-off.
