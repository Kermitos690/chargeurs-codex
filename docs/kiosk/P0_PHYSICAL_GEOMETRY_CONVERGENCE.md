# P0 physical geometry convergence

Field evidence: DTA21277 showed the shared system footer overlaying downstream CTAs, Home cards still inheriting a 760px max-width, weak pricing hierarchy and the right-side ambient wave appearing dead/absent.

This lane is presentation-only. The final CSS authority constrains the product layer above the footer, removes the inherited Home max-width, enlarges physical-distance typography and reactivates right-side ambient motion. No payment, rental, return, slot, ejection or hardware behavior is changed.
