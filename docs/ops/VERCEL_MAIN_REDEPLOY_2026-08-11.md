# Vercel main redeploy trigger — 2026-08-11

Purpose: force the Git→Vercel integration to build the current `main` after the Kiosk UX P1 merge, because the release workflow skipped deployment when Vercel credentials were unavailable in GitHub Actions.

This commit is documentation-only and changes no application, pricing, payment, rental, inventory, hardware, or advertising behavior.
