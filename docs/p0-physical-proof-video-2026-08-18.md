# P0 physical proof — DTA21277 video 2026-08-18

Physical video evidence after the geometry convergence showed one remaining deterministic presentation defect: the legacy Home footer and the new shared system footer were both visible simultaneously. The root cause is CSS specificity: the legacy Home footer rule uses a more specific selector with `display:flex !important`, so the previous generic `display:none !important` did not win.

The physical-proof lock matches that selector exactly, hides the obsolete Home footer, increases standing-distance price/body typography, and strengthens the right-side living ribbon. No payment, rental, Terminal, QR, return, slot, ejection or hardware logic changes are included.
