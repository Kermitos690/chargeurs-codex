# DTA21269 v3 field APK build

CI-only support for issue #288. This workflow builds the existing Stripe Terminal 3.0.0 USB hotfix branch into a canonically signed STAGING APK so the reconnect/cancellation fixes can actually reach the physical kiosk.

No pricing, deposit, capture, settlement, rental, return, Ads/Home or hardware-ejection behavior is changed by this branch.
