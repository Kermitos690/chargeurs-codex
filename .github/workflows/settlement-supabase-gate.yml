name: Chargeurs.ch Settlement Supabase Gate

on:
  workflow_dispatch:
    inputs:
      project_ref:
        description: "Supabase project ref explicitly targeted by this run"
        required: true
        default: "zoybkzkvvsbnqqarlaii"
        type: string
      apply_migrations:
        description: "Apply pending migrations after the dry-run"
        required: true
        default: false
        type: boolean
      deploy_functions:
        description: "Deploy the four settlement-related Edge Functions"
        required: true
        default: false
        type: boolean
      confirmation:
        description: "Required for writes: APPLY_CHARGEURS_SETTLEMENT"
        required: false
        default: ""
        type: string

permissions:
  contents: read

concurrency:
  group: chargeurs-settlement-supabase-gate
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_REF: ${{ inputs.project_ref }}
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate encrypted configuration and write intent
        shell: bash
        run: |
          set -euo pipefail
          : > settlement-gate-report.txt

          missing=()
          for name in SUPABASE_ACCESS_TOKEN SUPABASE_DB_PASSWORD; do
            if [ -z "${!name:-}" ]; then
              missing+=("$name")
            fi
          done
          if [ "${#missing[@]}" -gt 0 ]; then
            echo "Missing encrypted GitHub Actions secrets: ${missing[*]}" >&2
            echo "CONFIGURATION_READY=false" >> settlement-gate-report.txt
            exit 1
          fi

          if { [ "${{ inputs.apply_migrations }}" = "true" ] || [ "${{ inputs.deploy_functions }}" = "true" ]; } \
             && [ "${{ inputs.confirmation }}" != "APPLY_CHARGEURS_SETTLEMENT" ]; then
            echo "Write operations require the exact confirmation phrase." >&2
            echo "WRITE_CONFIRMATION=false" >> settlement-gate-report.txt
            exit 1
          fi

          echo "CONFIGURATION_READY=true" >> settlement-gate-report.txt
          echo "PROJECT_REF=$SUPABASE_PROJECT_REF" >> settlement-gate-report.txt
          echo "APPLY_MIGRATIONS=${{ inputs.apply_migrations }}" >> settlement-gate-report.txt
          echo "DEPLOY_FUNCTIONS=${{ inputs.deploy_functions }}" >> settlement-gate-report.txt

      - name: Link the explicitly selected project
        shell: bash
        run: |
          set -euo pipefail
          supabase link \
            --project-ref "$SUPABASE_PROJECT_REF" \
            --password "$SUPABASE_DB_PASSWORD"
          echo "PROJECT_LINKED=true" >> settlement-gate-report.txt

      - name: Inspect migration history
        shell: bash
        run: |
          set -euo pipefail
          supabase migration list --linked --password "$SUPABASE_DB_PASSWORD" \
            | tee migration-list-before.txt

      - name: Dry-run pending migrations
        shell: bash
        run: |
          set -euo pipefail
          supabase db push \
            --linked \
            --password "$SUPABASE_DB_PASSWORD" \
            --include-all \
            --dry-run \
            | tee migration-dry-run.txt
          echo "MIGRATION_DRY_RUN=true" >> settlement-gate-report.txt

      - name: Apply migrations
        if: inputs.apply_migrations == true
        shell: bash
        run: |
          set -euo pipefail
          supabase db push \
            --linked \
            --password "$SUPABASE_DB_PASSWORD" \
            --include-all \
            --yes \
            | tee migration-push.txt
          echo "MIGRATIONS_APPLIED=true" >> settlement-gate-report.txt

      - name: Verify final migration history
        if: inputs.apply_migrations == true
        shell: bash
        run: |
          set -euo pipefail
          supabase migration list --linked --password "$SUPABASE_DB_PASSWORD" \
            | tee migration-list-after.txt
          grep -q "20260715041000" migration-list-after.txt
          grep -q "20260715042000" migration-list-after.txt
          echo "SETTLEMENT_MIGRATIONS_PRESENT=true" >> settlement-gate-report.txt

      - name: Lint linked public schema
        if: inputs.apply_migrations == true
        shell: bash
        run: |
          set -euo pipefail
          supabase db lint \
            --linked \
            --schema public \
            --level error \
            --fail-on error \
            | tee database-lint.txt
          echo "DATABASE_LINT_PASSED=true" >> settlement-gate-report.txt

      - name: Deploy settlement Edge Functions
        if: inputs.deploy_functions == true
        shell: bash
        run: |
          set -euo pipefail
          for function_name in \
            create-stripe-checkout \
            stripe-webhook \
            settle-rental-payment \
            chargenow-rent-callback; do
            supabase functions deploy "$function_name" \
              --project-ref "$SUPABASE_PROJECT_REF" \
              --no-verify-jwt \
              --use-api
          done
          echo "SETTLEMENT_FUNCTIONS_DEPLOYED=true" >> settlement-gate-report.txt

      - name: Upload sanitized diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: settlement-supabase-gate-diagnostics
          path: |
            settlement-gate-report.txt
            migration-list-before.txt
            migration-dry-run.txt
            migration-push.txt
            migration-list-after.txt
            database-lint.txt
          if-no-files-found: ignore
          retention-days: 7
