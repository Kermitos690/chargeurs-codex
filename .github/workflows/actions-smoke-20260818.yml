name: Actions startup smoke 20260818

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/actions-smoke-20260818.yml

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner started
        run: echo "ACTIONS_RUNNER_STARTED"
