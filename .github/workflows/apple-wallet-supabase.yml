name: Chargeurs.ch Apple Wallet Supabase (paused)

# Emergency cost control: scheduled and deployment jobs are disabled.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo "Apple Wallet automation is paused for cost control."
