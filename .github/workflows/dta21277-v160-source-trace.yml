name: DTA21277 v160 source trace retired

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/dta21277-v160-source-trace.yml"

permissions:
  contents: read

concurrency:
  group: dta21277-v160-source-trace
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: [self-hosted, macOS]
    steps:
      - run: echo "Trace retired; v161 uses the verified USB lineage from 31ca0e/daf955."
