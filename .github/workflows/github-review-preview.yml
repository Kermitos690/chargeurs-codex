name: Chargeurs.ch GitHub Review Preview (paused obsolete PR14)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR #14 is obsolete and superseded by PR #16."
