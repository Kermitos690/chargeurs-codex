name: Chargeurs.ch Refonte V2 CI (superseded branch)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "This superseded branch is manual-only. Use PR #16 for canonical settlement work."
