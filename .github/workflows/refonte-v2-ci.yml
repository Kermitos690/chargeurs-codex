name: Chargeurs.ch Refonte V2 CI (paused)

# Emergency cost control: no automatic CI.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo "Automatic CI is paused for cost control."
