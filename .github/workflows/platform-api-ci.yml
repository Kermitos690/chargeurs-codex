name: Chargeurs.ch Platform API CI (superseded branch)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "This superseded branch is manual-only. Use the canonical integration PR for further work."
